meta {
  name: List Libraries
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/libraries
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("should return 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
