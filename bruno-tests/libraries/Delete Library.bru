meta {
  name: Delete Library
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/libraries/{{libraryId}}
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("should return 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}

docs {
  Delete a library by ID. Requires admin role. Returns 204 No Content on success.
}
